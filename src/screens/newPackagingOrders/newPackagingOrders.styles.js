import {StyleSheet} from 'react-native';

const NewPackagingOrdersScreenStyles = StyleSheet.create({
  scannerTopContent: {
    textAlign: 'center',
    marginHorizontal: 20,
  },
});

export default NewPackagingOrdersScreenStyles;
