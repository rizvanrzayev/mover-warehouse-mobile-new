import {StyleSheet} from 'react-native';

const SendingsSackSortingScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
});

export default SendingsSackSortingScreenStyles;
