import {StyleSheet} from 'react-native';

const OpenWarehouseSackScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
});

export default OpenWarehouseSackScreenStyles;
