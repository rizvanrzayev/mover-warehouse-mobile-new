import {StyleSheet} from 'react-native';

const WarehouseScreenStyles = StyleSheet.create({
  topContentContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  alertTitle: {},
  alertMessage: {
    fontSize: 25,
  },
  topContentContent: {
    justifyContent: 'space-evenly',
    alignItems: 'center',
    flex: 1,
  },
  topContentSectionContainer: {
    alignItems: 'center',
    // marginBottom: 20,
  },
});

export default WarehouseScreenStyles;
