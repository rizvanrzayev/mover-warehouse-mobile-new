import {StyleSheet} from 'react-native';

const NewSackingOrdersScreenStyles = StyleSheet.create({
  scannerTopContentContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default NewSackingOrdersScreenStyles;
