import {StyleSheet} from 'react-native';

const WarehouseSendingsScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
});

export default WarehouseSendingsScreenStyles;
