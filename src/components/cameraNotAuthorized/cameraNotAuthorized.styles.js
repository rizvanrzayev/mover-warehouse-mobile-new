import {StyleSheet} from 'react-native';

const CameraNotAuthorizedStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  refreshAuth: {
    marginTop: 10,
  },
});

export default CameraNotAuthorizedStyles;
