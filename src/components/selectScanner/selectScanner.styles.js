import {StyleSheet} from 'react-native';

const SelectScannerStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  selectScannerButton: {
    marginTop: 10,
  },
});

export default SelectScannerStyles;
